---
---

<section class="hero">

  <h1>Ernesto Cisnero</h1>
  <p class="role">Independent iOS Developer</p>

  <p class="bio">
    Building clean, native iOS apps with a focus on simplicity and usability.
  </p>

  
</section>

<style>
  .hero {
    padding: 64px 0 48px;
    max-width: 560px;
  }

  .avatar {
    width: 64px;
    height: 64px;
    border-radius: 18px;
    background: var(--accent);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 22px;
    font-weight: 600;
    letter-spacing: -0.01em;
    margin-bottom: 24px;
    box-shadow: 0 6px 16px rgba(0, 122, 255, 0.25);
  }

  h1 {
    font-size: 2rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 4px;
    color: var(--ink);
  }

  .role {
    font-size: 1rem;
    color: var(--ink-soft);
    margin-bottom: 20px;
  }

  .bio {
    font-size: 1.05rem;
    line-height: 1.6;
    color: var(--ink-soft);
    margin-bottom: 28px;
    max-width: 48ch;
  }

  .contact-row {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
  }

  .pill {
    display: inline-flex;
    align-items: center;
    padding: 8px 16px;
    border-radius: 999px;
    background: #fff;
    border: 1px solid var(--hairline);
    color: var(--ink);
    font-size: 0.9rem;
    font-weight: 500;
    text-decoration: none;
    transition: border-color 150ms ease, background 150ms ease;
  }

  .pill:hover {
    border-color: var(--accent);
    background: rgba(0, 122, 255, 0.06);
  }
</style>